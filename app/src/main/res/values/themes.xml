<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Snaptick" parent="android:Theme.Material.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:statusBarColor">#000</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#161A30</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_app_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Snaptick</item>
    </style>

</resources>